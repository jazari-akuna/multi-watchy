// SimThermometer — all inline in the header. This TU exists so the file set
// is consistent and the Makefile glob picks it up without conditionals
// (matches the SimButtons.cpp pattern).

#include "SimThermometer.h"
